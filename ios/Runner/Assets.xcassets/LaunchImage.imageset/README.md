# Launch Screen Assets

Solid Atlas `warmOffWhite` placeholders used with `LaunchScreen.storyboard`
(centred « Atlas » title). Replace with a branded still if needed — keep
background close to `#FAF7F2` to avoid a flash before first Flutter frame.
